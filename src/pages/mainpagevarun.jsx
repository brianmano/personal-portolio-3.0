import React, { useState, useEffect, useRef } from 'react';
import {
  ChakraProvider, Box, VStack, HStack, SimpleGrid, Select, Button, Checkbox, 
  Text, Divider, Slider, SliderTrack, SliderFilledTrack, SliderThumb, SliderMark, Heading, 
  Modal, ModalOverlay, ModalContent, ModalHeader, ModalFooter, ModalBody, ModalCloseButton,
  Spinner,
} from '@chakra-ui/react';
// import { CheckIcon, CloseIcon  } from '@chakra-ui/icons';

const MainPageVarun = () => {

  return (
    <ChakraProvider>
        <Text>
            test
        </Text>
    </ChakraProvider>
  );
};

export default MainPageVarun;
